import React, { PropTypes } from 'react'

const Switch = React.createClass({
  getInitialState () {
    return {} // The initial state
  },

  render () {
    return <div><em>Bdog will be here</em></div>
  }
})

module.exports = Switch
